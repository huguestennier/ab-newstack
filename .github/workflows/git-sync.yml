on: push
jobs:
  git-sync:
    runs-on: ubuntu-latest
    steps:
      - name: git-sync
        uses: wei/git-sync@v3
        with:
          source_repo: "git@github.com:huguestennier/ab-newstack.git"
          source_branch: "master"
          destination_repo: "git@github.com:huguestennier/ab-newstack.git"
          destination_branch: "pricing-b"